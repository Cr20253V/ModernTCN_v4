 Fig. 1 图注建议稍微具体一点

现在的 Fig. 1 图注是正确的，但略偏概括。考虑到图中已经明确画出了三任务输出、scheduling conditioning 和 LPV-MPC scheduler，我建议图注再稍微具体一些，突出“神经网络不是直接控制器”这一点。

建议改成：

```latex
\caption{Overall framework of the proposed temporal-perception-enhanced LPV-MPC.
The ModernTCN-based estimator infers the main operating condition, steering direction,
and slope-related scheduling estimate from a historical proprioceptive window. The
slope-related estimate is conditioned and incorporated into the LPV-MPC scheduling
vector, while the final control commands are generated by the constrained LPV-MPC
controller.}
```

这比当前版本更完整，也更符合你的创新点。

## 3. Fig. 2 图注可以再补充变量解释

当前 Fig. 2 图注只解释了 LF/RR 和 RF/LR 的主动/被动关系。这个版本是可以的，但图中还包含 (v)、(\beta)、(\psi)、(\omega)、(\delta_{lf})、(\delta_{rr})、(F_{x,lf})、(F_{x,rr})、(L)、(W)、(\theta) 等变量。为了让图注更自洽，建议扩展一点。

建议改成：

```latex
\caption{Configuration and motion variables of the diagonal dual-steer AGV.
The LF and RR wheels are active drive-steer wheels, whereas the RF and LR wheels are
passive support wheels. The variables $v$, $\beta$, $\psi$, $\omega$, $\delta_{lf}$,
$\delta_{rr}$, $F_{x,lf}$, $F_{x,rr}$, $L$, $W$, and $\theta$ denote the longitudinal
velocity, sideslip angle, heading angle, yaw rate, steering angles, driving forces,
vehicle length, track width, and road grade angle, respectively.}
```
