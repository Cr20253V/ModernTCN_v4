# D1 CLI Contract

- pass: `1`
- dataset_policy: `formal exp3 commands must pass --dataset-file explicitly; full default is not trusted as the experiment contract.`
- supported_aliases: `hyphen and underscore variants for model-family, patch-size, patch-stride, stage-blocks, large-kernels, small-kernels, no-overwrite`
- output_root: `results/modern_tcn_ablation/exp3_patch_full`
- no_overwrite: `required for smoke and formal runs`
