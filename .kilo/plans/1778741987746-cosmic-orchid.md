# Plan: 上传项目到 GitHub (ModernTCN_v1)

## Context

当前仓库 `E:\Matlab\Simulink\S-Function_16` 已有 git 初始化，分支 `main` 跟踪 `github_1/main`（ahead 2 commits）。用户希望将**全部内容**推送到 `ModernTCN_v1` remote (`https://github.com/Cr20253V/ModernTCN_v1.git`)，该 remote 目前为空。

当前有未提交的修改：
- **modified**: `results/tcn/.../TCN_theta10_v2_multi_seed_summary_partial.csv`, `src/Mamba/model/mamba` (submodule)
- **untracked**: `.kilo/`, `data/models/TCN_*.mat`, `results/tcn/.../seed11/`

## Steps

### 1. 暂存所有变更
```bash
git add -A
```
将所有 modified、untracked 文件加入暂存区。

### 2. 提交变更
```bash
git commit -m "chore: stage all changes for ModernTCN_v1 upload"
```

### 3. 推送到 ModernTCN_v1 remote
由于 `ModernTCN_v1` 是空仓库（无任何分支），需要设置 upstream 并推送：
```bash
git push -u ModernTCN_v1 main
```

这将把本地 `main` 分支（包含所有历史和最新提交）推送到 `ModernTCN_v1`。

## 注意事项

- `.kilo/` 目录将被包含在提交中（当前 `.gitignore` 未忽略它）。如需排除，可在提交前添加到 `.gitignore`。
- `data/models/*.mat` 文件也会被提交。
- `src/Mamba/model/mamba` 是 submodule，其 modified content 会被记录。
- 推送时可能需要 GitHub 认证（token 或 SSH key）。

## Verification

- 推送完成后，运行 `git remote -v` 确认 remote 配置
- 运行 `git status` 确认工作区干净
- 访问 `https://github.com/Cr20253V/ModernTCN_v1` 确认文件已上传
